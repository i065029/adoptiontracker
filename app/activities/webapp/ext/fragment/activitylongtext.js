/**
 * This is the handler function for the long text activity on the Activities
 * object page.
 * 
 * NOTE: It is not currently required because we instantiate a new
 * view in the object page section extension fragment.
 * This view has its own controller.
 */
sap.ui.define([], function() {
    'use strict';

    return {
        predefinedMethod: function() {}
    };
});
